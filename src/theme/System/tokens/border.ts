// Used
const borderRadius = {
  s: '8px',
  base: '12px',
  m: '32px',
};

export type SystemBorderRadiusMap = typeof borderRadius;
export type SystemBorderRadius = keyof SystemBorderRadiusMap;
export default borderRadius;
